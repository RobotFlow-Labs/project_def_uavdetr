# Task PRD-0202: Implement SWSA-IFI Encoder

> PRD: PRD-02 | Depends: PRD-0201
> Files: 2 | Lines: ~150 | Time: ~75min

## What to Build
Implement the sliding-window self-attention encoder used on the projected `S5` feature map. Preserve the `[B, 256, 20, 20]` shape.

## Files

### Create: `src/anima_def_uavdetr/models/encoder.py`
```python
class SWSAIFIEncoder(nn.Module):
    def forward(self, x):
        return x + self.ffn(self.swsa(x))
```

### Create: `tests/test_encoder.py`
```python
def test_encoder_preserves_shape():
    x = torch.randn(2, 256, 20, 20)
    assert encoder(x).shape == x.shape
```

## Verify
```bash
uv run pytest tests/test_encoder.py -v
```
