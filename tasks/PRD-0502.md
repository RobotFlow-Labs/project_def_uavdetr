# Task PRD-0502: Wire Prediction Endpoint

> PRD: PRD-05 | Depends: PRD-0501
> Files: 1 | Lines: ~100 | Time: ~45min

## What to Build
Connect the predictor to `POST /predict` and return detection JSON with `bbox_xyxy`, `score`, `class_id`, and `label`.

## Files

### Update: `src/anima_def_uavdetr/api/app.py`
```python
@app.post("/predict")
async def predict(file: UploadFile, conf: float = 0.25):
    ...
```

## Verify
```bash
uv run pytest tests/test_api.py -v
```
