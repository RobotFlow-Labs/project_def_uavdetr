# Task PRD-0102: Implement Typed Settings

> PRD: PRD-01 | Depends: PRD-0101
> Files: 3 | Lines: ~120 | Time: ~60min

## What to Build
Create a Pydantic settings object for dataset roots, runtime backend, image size, checkpoint paths, and export destinations. Keep the defaults aligned with this repo and the paper.

## Files

### Create: `src/anima_def_uavdetr/config.py`
```python
from pathlib import Path
from pydantic_settings import BaseSettings


class UavDetrSettings(BaseSettings):
    image_size: int = 640
    frame_sample_stride: int = 5
    uav_dataset_root: Path
    dut_dataset_root: Path
```

### Create: `tests/test_config.py`
```python
def test_default_image_size():
    assert UavDetrSettings.model_fields["image_size"].default == 640
```

## Verify
```bash
uv run pytest tests/test_config.py -v
```
