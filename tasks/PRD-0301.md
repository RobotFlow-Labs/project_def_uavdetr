# Task PRD-0301: Add Checkpoint I/O

> PRD: PRD-03 | Depends: PRD-0204
> Files: 2 | Lines: ~110 | Time: ~45min

## What to Build
Add checkpoint save/load helpers for scratch training, resumed training, and inference-only load paths.

## Files

### Create: `src/anima_def_uavdetr/checkpoints.py`
```python
def load_checkpoint(model, path):
    state = torch.load(path, map_location="cpu")
    model.load_state_dict(state["model"] if "model" in state else state)
    return model
```

### Create: `tests/test_checkpoint_io.py`
```python
def test_checkpoint_loader_accepts_plain_state_dict(tmp_path):
    ...
```

## Verify
```bash
uv run pytest tests/test_checkpoint_io.py -v
```
