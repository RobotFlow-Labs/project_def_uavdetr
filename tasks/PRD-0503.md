# Task PRD-0503: Build Docker and Compose Config

> PRD: PRD-05 | Depends: PRD-0502
> Files: 3 | Lines: ~160 | Time: ~60min

## What to Build
Create the API container, compose file, and example environment file with explicit checkpoint mounts.

## Files

### Create: `docker/Dockerfile.api`
```dockerfile
FROM python:3.11-slim
WORKDIR /app
```

### Create: `docker-compose.api.yml`
```yaml
services:
  def-uavdetr-api:
    build:
      context: .
      dockerfile: docker/Dockerfile.api
```

### Create: `.env.example`
```bash
ANIMA_BACKEND=cuda
DEF_UAVDETR_CHECKPOINT=/models/uavdetr-best.pt
```

## Verify
```bash
uv run pytest tests/test_container_config.py -v
```
