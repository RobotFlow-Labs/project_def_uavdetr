# Task PRD-0104: Add Foundation Tests

> PRD: PRD-01 | Depends: PRD-0103
> Files: 2 | Lines: ~90 | Time: ~45min

## What to Build
Finish the foundation layer by adding fixture-backed tests for settings, dataset manifests, and frame sampling. This task closes PRD-01.

## Files

### Update: `tests/test_config.py`
```python
def test_settings_accept_explicit_paths(tmp_path):
    settings = UavDetrSettings(uav_dataset_root=tmp_path, dut_dataset_root=tmp_path)
    assert settings.uav_dataset_root == tmp_path
```

### Update: `tests/test_dataset_manifest.py`
```python
def test_layout_uses_expected_split_names():
    assert {"train", "val", "test"} == {"train", "val", "test"}
```

## Verify
```bash
uv run pytest tests/test_config.py tests/test_dataset_manifest.py -v
uv run ruff check src/ tests/
```
