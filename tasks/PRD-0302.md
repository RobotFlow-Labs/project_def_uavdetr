# Task PRD-0302: Add Postprocess and Predictor

> PRD: PRD-03 | Depends: PRD-0301
> Files: 3 | Lines: ~180 | Time: ~75min

## What to Build
Implement query decoding, confidence filtering, and the Python prediction surface that wraps the end-to-end model.

## Files

### Create: `src/anima_def_uavdetr/postprocess.py`
```python
def postprocess_queries(pred_boxes, pred_logits, conf=0.25, max_det=300):
    return detections
```

### Create: `src/anima_def_uavdetr/infer.py`
```python
class DefUavDetrPredictor:
    def predict(self, images, conf=0.25):
        ...
```

### Create: `tests/test_infer.py`
```python
def test_predictor_returns_xyxy_score_class():
    ...
```

## Verify
```bash
uv run pytest tests/test_infer.py -v
```
