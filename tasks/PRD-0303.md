# Task PRD-0303: Add CLI Inference

> PRD: PRD-03 | Depends: PRD-0302
> Files: 1 | Lines: ~80 | Time: ~45min

## What to Build
Add a clean CLI for image-folder inference that replaces the hard-coded `detect.py` flow in the vendored repo.

## Files

### Create: `scripts/run_infer.py`
```python
if __name__ == "__main__":
    args = parse_args()
    predictor = build_predictor(args)
    predictor.predict_from_path(args.source, conf=args.conf)
```

## Verify
```bash
uv run python scripts/run_infer.py --help
```
