# Task PRD-0501: Build FastAPI Schemas and App

> PRD: PRD-05 | Depends: PRD-0302
> Files: 2 | Lines: ~140 | Time: ~60min

## What to Build
Create the FastAPI app shell, request/response schemas, and health/readiness endpoints.

## Files

### Create: `src/anima_def_uavdetr/api/schemas.py`
```python
class DetectionResponse(BaseModel):
    detections: list[dict]
```

### Create: `src/anima_def_uavdetr/api/app.py`
```python
app = FastAPI()
```

## Verify
```bash
uv run python -c "from anima_def_uavdetr.api.app import app; print(app.title)"
```
