# Task PRD-0201: Implement WTConv Backbone

> PRD: PRD-02 | Depends: PRD-0104
> Files: 2 | Lines: ~220 | Time: ~90min

## What to Build
Port the WTConv-backed backbone stages into the module package. The target output shapes at `640x640` are `S2..S5` with strides `4/8/16/32`.

## Files

### Create: `src/anima_def_uavdetr/models/backbone.py`
```python
class WTConvBackbone(nn.Module):
    def forward(self, x):
        s2 = ...
        s3 = ...
        s4 = ...
        s5 = ...
        return s2, s3, s4, s5
```

### Create: `tests/test_backbone.py`
```python
def test_backbone_feature_shapes():
    images = torch.randn(2, 3, 640, 640)
    s2, s3, s4, s5 = model(images)
    assert s5.shape[-2:] == (20, 20)
```

## Verify
```bash
uv run pytest tests/test_backbone.py -v
```
