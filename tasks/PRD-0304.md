# Task PRD-0304: Add Export Hooks

> PRD: PRD-03 | Depends: PRD-0303
> Files: 2 | Lines: ~120 | Time: ~60min

## What to Build
Add export entrypoints for PyTorch and ONNX, with extension points for TensorRT and MLX adaptation.

## Files

### Create: `scripts/export.py`
```python
def export_onnx(model, path):
    torch.onnx.export(model, example_input, path)
```

### Create: `tests/test_export.py`
```python
def test_export_registry_contains_onnx():
    assert "onnx" in SUPPORTED_EXPORTS
```

## Verify
```bash
uv run pytest tests/test_export.py -v
```
