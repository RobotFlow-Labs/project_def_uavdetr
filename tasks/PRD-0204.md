# Task PRD-0204: Integrate Decoder and Hybrid Loss

> PRD: PRD-02 | Depends: PRD-0203
> Files: 4 | Lines: ~260 | Time: ~90min

## What to Build
Wrap the RT-DETR decoder path and implement the Inner-CIoU + NWD hybrid loss. Close PRD-02 with an end-to-end forward test.

## Files

### Create: `src/anima_def_uavdetr/models/decoder.py`
```python
class RTDETRHead(nn.Module):
    def forward(self, pyramid_features, targets=None):
        return pred_boxes, pred_logits
```

### Create: `src/anima_def_uavdetr/losses.py`
```python
class HybridInnerCiouNwdLoss(nn.Module):
    def forward(self, pred_boxes, pred_logits, targets):
        return {"loss": total_loss}
```

### Create: `src/anima_def_uavdetr/model.py`
```python
class DefUavDetr(nn.Module):
    ...
```

### Create: `tests/test_model.py`
```python
def test_model_emits_300_queries():
    boxes, logits = model(images)
    assert boxes.shape[1] == 300
```

## Verify
```bash
uv run pytest tests/test_backbone.py tests/test_encoder.py tests/test_neck.py tests/test_model.py tests/test_losses.py -v
```
