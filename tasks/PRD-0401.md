# Task PRD-0401: Implement Metrics Core

> PRD: PRD-04 | Depends: PRD-0304
> Files: 2 | Lines: ~140 | Time: ~60min

## What to Build
Implement reusable precision/recall/F1/mAP helpers and paper target constants for the two benchmark families.

## Files

### Create: `src/anima_def_uavdetr/metrics.py`
```python
PAPER_TARGETS = {"custom_uav": {"map50_95": 62.56}, "dut_anti_uav": {"map50_95": 67.15}}
```

### Create: `tests/test_metrics.py`
```python
def test_paper_targets_include_both_datasets():
    assert set(PAPER_TARGETS) == {"custom_uav", "dut_anti_uav"}
```

## Verify
```bash
uv run pytest tests/test_metrics.py -v
```
