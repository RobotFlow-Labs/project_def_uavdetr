# Task PRD-0402: Implement Custom UAV Evaluator

> PRD: PRD-04 | Depends: PRD-0401
> Files: 1 | Lines: ~160 | Time: ~75min

## What to Build
Add the custom UAV evaluator that consumes the staged 7:2:1 test split and emits the full paper metric set.

## Files

### Create: `src/anima_def_uavdetr/eval/custom_uav.py`
```python
def evaluate_custom_uav(model, dataset_root):
    return {"precision": ..., "map50_95": ...}
```

## Verify
```bash
uv run python -m anima_def_uavdetr.eval.custom_uav --help
```
