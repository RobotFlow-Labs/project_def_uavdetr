# DEF-UAVDETR — Docker serving environment
ANIMA_MODULE_NAME=def-uavdetr
ANIMA_MODULE_VERSION=0.1.0
ANIMA_HF_REPO=ilessio-aiflowlab/project_def_uavdetr
ANIMA_DEVICE=auto
ANIMA_WEIGHT_FORMAT=auto
ANIMA_SERVE_PORT=8080
ANIMA_WEIGHT_DIR=/data/weights
ANIMA_DATASET_DIR=/data/datasets
# HF_TOKEN=  # set at runtime for private repos
